# If you need to add another file built from configuration, you may add it
# here. `npx envsub` will replace all `${VAR_NAME}` with a corresponding
# environment variable.

npx envsub src/environments/build.config.ts src/environments/environment.ts